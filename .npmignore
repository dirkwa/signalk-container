src/
dist/test/
# configpanel TSX is bundled by vite into public/; tsc excludes
# src/configpanel from emit, but the rule stays as a safety belt
# in case the exclude is ever removed.
dist/configpanel/
# Build-time guard against hardcoded home paths; runs from the repo, never
# from an installed copy.
dist/scripts/
.github/
*.tsbuildinfo
.DS_Store
eslint.config.js
tsconfig.json
vite.config.ts

# node --test JUnit report; a local artifact, never published
test-results/
